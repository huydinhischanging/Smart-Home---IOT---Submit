# Procfile — Railway / Heroku / Render deployment
# SocketIO yêu cầu eventlet và -w 1 (1 worker, không phải multi-process)
web: gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT --timeout 120 wsgi:application
